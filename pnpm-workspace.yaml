# Single-package "workspace" — only the repo root is a member. The
# empty `packages:` list keeps nested directories like `report/ui`
# (cloned by scripts/build-ui.sh) from being treated as workspace
# members, which would break their self-contained `pnpm install`.
packages: []
